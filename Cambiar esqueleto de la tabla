alter table authors modify column birthyear year default 1920;

alter table authors add column birthyear integer default 1930 after name;

alter authors drop column birthdate;  

mysqldump -u root -p cursoplatzi respaldo local en el sistema 

mysqldump -u root -p -d cursoplatzi > esquema.sql ## Exportar el esquema en el equipo 
